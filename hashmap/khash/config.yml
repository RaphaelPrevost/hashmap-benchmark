name: khash
display_name: khash (C)

source:
  type: github-raw
  repo: attractivechaos/klib
  ref: master   # later: replace with pinned commit hash
  files:
    - khash.h

build:
  system: cc
  compiler: cc
  cflags:
    - -O3
    - -DNDEBUG
    - -std=c11
    - -W
    - -Wall
    - -Wpointer-arith
  include_dirs:
    - .
  sources:
    - benchmark.c
  output: hashbench
